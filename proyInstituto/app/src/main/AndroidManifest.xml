<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyinstituto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".verAlumnosBD"></activity>
        <activity android:name=".verAsignaturas" />
        <activity android:name=".altaAsig" />
        <activity android:name=".verProfesores" />
        <activity android:name=".altaProfesor" />
        <activity android:name=".verAlumnos" />
        <activity android:name=".altaAlum" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>